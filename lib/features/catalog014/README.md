# 014. SharedPreferences

## 📘 概要

この画面では **[SharedPreferences]** の実装例を確認できます。
Flutter での基本的な使い方、およびカスタマイズ方法も含まれています。
初回の遷移では初回専用ページが表示され、ローカルデータにフラグがある状態では別のページが表示されるようになっているサンプルです
アプリ起動時のチュートリアル表示だしわけ等で使われます 

---

## 🔧 実装内容

- [✓] SharedPreferences

---

## 📁 関連ファイル

```
lib/
└── features/
    └── catalog014/
          └── view
            └── shared_preferences_once_page.dart
            └── shared_preferences_simple_page.dart
          └── shared_preferences_page.dart
```

---

## 🔍 検索用キーワード

- SharedPreferences
